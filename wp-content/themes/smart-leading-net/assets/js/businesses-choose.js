/**
 * Smart Leading Net — Businesses Choose section
 */
(function () {
	'use strict';

	function initBusinessesChoose() {
		// Reserved for future section interactions.
	}

	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', initBusinessesChoose);
	} else {
		initBusinessesChoose();
	}
})();
