# Block direct web access to CLI debug scripts (they echo plain text and must not run in HTTP context).
<IfModule mod_authz_core.c>
	Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
	Deny from all
</IfModule>
